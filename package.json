{
  "name": "@cortexkit/anthropic-auth",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/core && bun run build && cd ../opencode && bun run build && cd ../pi && bun run build",
    "dev": "bun scripts/dev.ts",
    "dev:clean": "bun scripts/dev-clean.ts",
    "extract": "bun scripts/extract-system-prompt.ts",
    "analyze:cache": "node scripts/analyze-cache-usage.mjs",
    "test": "bun run build && bun test scripts/version-sync.test.ts scripts/verify-artifacts.test.ts scripts/release.test.ts && cd packages/opencode && bun run test",
    "test:version-sync": "bun test scripts/version-sync.test.ts",
    "test:verify-artifacts": "bun test scripts/verify-artifacts.test.ts",
    "verify:artifacts": "node scripts/verify-artifacts.mjs",
    "verify:artifacts:manifests": "node scripts/verify-artifacts.mjs --manifests-only",
    "verify:artifacts:install": "node scripts/verify-artifacts.mjs --install-check",
    "verify:artifacts:registry": "node scripts/verify-artifacts.mjs --registry-tarball --manifests-only",
    "test:e2e": "cd packages/core && bun run build && cd ../.. && bun run --cwd packages/e2e-tests test",
    "typecheck": "cd packages/core && bun run build && cd ../opencode && bun run typecheck && cd ../pi && bun run typecheck && cd ../.. && tsc -p tsconfig.scripts.json",
    "types": "bun run typecheck",
    "format": "biome check --write --unsafe .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "pack:core:dry": "cd packages/core && bun pm pack --dry-run",
    "pack:opencode:dry": "cd packages/opencode && bun pm pack --dry-run",
    "pack:pi:dry": "cd packages/pi && bun pm pack --dry-run",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@earendil-works/pi-ai": "0.75.3",
    "@earendil-works/pi-coding-agent": "0.75.3",
    "@earendil-works/pi-tui": "0.75.3",
    "@opencode-ai/plugin": "1.15.5",
    "@tsconfig/bun": "1.0.10",
    "@types/bun": "1.3.14",
    "dedent": "^1.7.2",
    "lefthook": "2.1.6",
    "miniflare": "^4.20260515.0",
    "typescript": "6.0.3"
  }
}
